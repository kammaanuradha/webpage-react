import React from 'react';
import { Link } from 'react-router-dom';
import { Header } from './components/Header';

const Home = () => {
    return (
        <div>
          <h3 style={{color:'white'}}>Welcome to Home page</h3>
        </div>
    );
};

export default Home;
